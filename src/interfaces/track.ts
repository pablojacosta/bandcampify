/* eslint-disable @typescript-eslint/no-explicit-any */
export interface ITrack {
  handleOnPlayClick: any;
  name: string;
  artist: string;
  albumId: string;
  albumName: string;
  albumUrl: string;
  index: number;
  id: string;
}
